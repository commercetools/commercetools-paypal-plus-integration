# settings from /config/application.yml re-write settings from /application.yml
# rename to application.yml and fill-in to use it in local run/debug mode

logging.level.com.paypal: INFO

tenantConfig:
  tenants:
    paypalplus-dev:
      ctp:
        projectKey: paypalplus-dev
        clientId:
        clientSecret:
      paypalPlus:
        id:
        secret:
        mode: sandbox

ctp.paypal.plus.integration.server.url: https://integration-server-url.com